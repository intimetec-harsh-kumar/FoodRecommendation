{
  "name": "server",
  "version": "1.0.0",
  "description": "this is a server",
  "main": "dist/server.js",
  "scripts": {
    "start": "npm run build && node dist/server.js",
    "build": "tsc"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mysql2": "^3.10.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "@types/socket.io": "^3.0.2"
  }
}
